# Frontend origin allowed to call the API
FRONTEND_ORIGIN=http://localhost:5173

# GitHub username used to surface repositories on the site
GITHUB_USERNAME=JsriVirinchi

# SMTP configuration for contact form.
# Provide valid credentials to forward messages to your inbox.
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=587
# EMAIL_SMTP_USERNAME=example@gmail.com
# EMAIL_SMTP_PASSWORD=app-specific-password
# EMAIL_FROM=portfolio@yourdomain.com
# EMAIL_FORWARD_TO=your-email@example.com
# EMAIL_USE_TLS=true
