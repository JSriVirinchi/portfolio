# Local dev points at the FastAPI server. For the AWS build, set this to the
# API domain at build time, e.g. VITE_API_BASE_URL=https://api.virinchijunuthula.com
VITE_API_BASE_URL=http://localhost:8000
